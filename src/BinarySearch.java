public class BinarySearch {
    public static int[] time (int[] arraySorted, int searchNumber){
        //define the data a top
        //use some alg to sort the array - we assume a sorted array
        int[] result = {10,20,30,40,50};
        //loops
        //edge case where there are multiple entries to te same  check if its the same as before
        //not to be stuck in a loop

        return result;
    }
}